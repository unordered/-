#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int Parser_LoadFile(const char* initFile);
int Parser_GetValue_Int(const char* tag, int* value);
int iValue1, iValue2;

int main(void)
{

	return 0;
}

int Parser_LoadFile(const char* initFile)
{
	return 0;
}

int Parser_GetValue_Int(const char* tag, int* value)
{
	return 0;
}
